# Инструкция по сборке расширения

## Режимы сборки

### 🛠️ Development (Dev) режим

В dev режиме доступны все функции, включая удаление данных:
- Кнопки "Удалить" и "Очистить" видны
- Можно удалять отдельные вопросы
- Можно очистить всю базу данных

**Сборка:**
```bash
npm run build:dev
```

### 🚀 Production режим

В production режиме **удаление данных отключено** для защиты базы данных:
- Кнопки "Удалить" и "Очистить" скрыты
- Невозможно удалить отдельные вопросы
- Невозможно очистить всю базу данных
- Все данные защищены от случайного удаления

**Сборка:**
```bash
npm run build:prod
# или просто
npm run build
```

## Настройка режима в коде

### React компоненты (src/)

Используется конфигурация из `src/config.ts`:
```typescript
import { ALLOW_DELETE_DATA } from '../config';

// Кнопки удаления показываются только если ALLOW_DELETE_DATA === true
{ALLOW_DELETE_DATA && (
  <button onClick={deleteItem}>Удалить</button>
)}
```

### JavaScript файлы (js/)

В файлах `js/background.js` и `js/saved-data.js` установите:
```javascript
const IS_DEV_MODE = false; // true для dev, false для production
```

## Проверка режима

После сборки проверьте:
1. Откройте `html/saved-data-react.html`
2. Убедитесь, что кнопки "Удалить" и "Очистить" отсутствуют (production)
3. Или присутствуют (dev)

## Важно

- **Для публикации** всегда используйте `npm run build:prod`
- **Для разработки** используйте `npm run build:dev`
- В production версии данные защищены от удаления

