# Развертывание на Cloudflare Workers

## ✅ Репозиторий готов!

**GitHub:** https://github.com/airccs/lms-mai

## 🚀 Развертывание на Cloudflare Workers:

### Шаг 1: Установите Wrangler CLI (если еще не установлен)

```powershell
npm install -g wrangler
```

### Шаг 2: Авторизуйтесь в Cloudflare

```powershell
wrangler login
```

Откроется браузер для авторизации.

### Шаг 3: Создайте KV Namespace (для постоянного хранения данных)

```powershell
wrangler kv:namespace create "QUIZ_DATA"
```

Скопируйте **ID** из вывода команды.

### Шаг 4: Обновите wrangler.toml

Откройте `wrangler.toml` и замените `YOUR_KV_NAMESPACE_ID` на реальный ID:

```toml
[[kv_namespaces]]
binding = "QUIZ_DATA"
id = "ваш-реальный-id-здесь"
```

### Шаг 5: Разверните Worker

```powershell
wrangler deploy
```

### Шаг 6: Получите URL

После развертывания вы получите URL вида:
```
https://lms-mai-api.ваш-username.workers.dev
```

### Шаг 7: Настройте расширение

В popup расширения:
- Включите "Синхронизацию с сервером"
- URL: `https://lms-mai-api.ваш-username.workers.dev`
- Сохраните

## ✅ Готово!

Теперь синхронизация работает для всех пользователей через Cloudflare Workers!

## Проверка работы:

Откройте в браузере:
```
https://lms-mai-api.ваш-username.workers.dev/api/health
```

Должен вернуться:
```json
{
  "status": "ok",
  "message": "Quiz Solver API is running"
}
```

## ⚠️ Важно:

- **Текущая версия использует хранение в памяти** - данные теряются при перезапуске Worker
- Для постоянного хранения нужно использовать KV (см. шаг 3) и обновить `worker.js` для работы с KV
- Cloudflare Workers имеют ограничения: 10ms CPU time на бесплатном плане, 50ms на платном

## Альтернатива: Cloudflare Pages с Functions

Если нужна более гибкая настройка, можно использовать Cloudflare Pages с Functions.

