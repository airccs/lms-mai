# Быстрый старт: Cloudflare Workers

## ✅ Файлы готовы!

Созданы файлы:
- `worker.js` - код Worker
- `wrangler.toml` - конфигурация
- `.github/workflows/cloudflare-deploy.yml` - автоматическое развертывание

## 🚀 Вариант 1: Ручное развертывание (быстро)

### 1. Установите Wrangler (если еще не установлен):
```powershell
npm install -g wrangler
```

### 2. Авторизуйтесь:
```powershell
wrangler login
```

### 3. Разверните:
```powershell
cd f:\lms
wrangler deploy
```

### 4. Получите URL:
После развертывания вы увидите URL вида:
```
https://lms-mai-api.ваш-username.workers.dev
```

## 🚀 Вариант 2: Автоматическое развертывание через GitHub

### 1. Получите Cloudflare API Token:
1. Зайдите на https://dash.cloudflare.com/profile/api-tokens
2. Нажмите "Create Token"
3. Используйте шаблон "Edit Cloudflare Workers"
4. Скопируйте токен

### 2. Получите Account ID:
1. Зайдите на https://dash.cloudflare.com/
2. Выберите любой домен
3. В правой панели найдите "Account ID"
4. Скопируйте ID

### 3. Добавьте Secrets в GitHub:
1. Зайдите в репозиторий: https://github.com/airccs/lms-mai
2. Settings → Secrets and variables → Actions
3. Добавьте:
   - `CLOUDFLARE_API_TOKEN` = ваш токен
   - `CLOUDFLARE_ACCOUNT_ID` = ваш Account ID

### 4. Запустите развертывание:
- При каждом push в `main` будет автоматическое развертывание
- Или вручную: Actions → Deploy to Cloudflare Workers → Run workflow

## ⚙️ Настройка расширения:

После получения URL:
1. Откройте popup расширения
2. Включите "Синхронизацию с сервером"
3. URL: `https://lms-mai-api.ваш-username.workers.dev`
4. Сохраните

## ✅ Проверка:

Откройте в браузере:
```
https://lms-mai-api.ваш-username.workers.dev/api/health
```

Должен вернуться:
```json
{
  "status": "ok",
  "message": "Quiz Solver API is running"
}
```

## ⚠️ Важно:

- **Текущая версия использует хранение в памяти** - данные теряются при перезапуске
- Для постоянного хранения используйте KV (см. `CLOUDFLARE_DEPLOY.md`)

