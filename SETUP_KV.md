# Настройка постоянного хранения (KV)

## ✅ Текущая ситуация:

**Worker работает постоянно** ✅
- Cloudflare обеспечивает 99.9% uptime
- Worker всегда доступен

**НО данные теряются** ❌
- Текущая версия (`worker.js`) использует память
- При перезапуске данные теряются

## 🔧 Решение: Обновить на KV версию

### Шаг 1: Создайте KV Namespace

```powershell
cd f:\lms
wrangler kv:namespace create "QUIZ_DATA"
```

Вы получите вывод вида:
```
🌀  Creating namespace with title "lms-mai-api-QUIZ_DATA"
✨  Success!
Add the following to your configuration file in your kv_namespaces array:
{ binding = "QUIZ_DATA", id = "abc123def456..." }
```

**Скопируйте ID!**

### Шаг 2: Обновите wrangler.toml

Откройте `wrangler.toml` и раскомментируйте/обновите секцию KV:

```toml
[[kv_namespaces]]
binding = "QUIZ_DATA"
id = "ваш-реальный-id-здесь"  # Вставьте ID из шага 1
```

### Шаг 3: Замените worker.js на версию с KV

```powershell
# Сделайте backup текущей версии
copy worker.js worker-memory.js

# Замените на KV версию
copy worker-kv.js worker.js
```

### Шаг 4: Разверните обновленный Worker

```powershell
wrangler deploy
```

## ✅ Готово!

Теперь данные будут сохраняться **навсегда** в Cloudflare KV!

## Проверка:

После развертывания откройте:
```
https://lms-mai.iljakir-06.workers.dev/api/health
```

Должно вернуться:
```json
{
  "status": "ok",
  "message": "Quiz Solver API is running",
  "storage": "KV (persistent)"
}
```

Если видите `"storage": "Memory (temporary)"` - значит KV не настроен правильно.

## Преимущества KV версии:

- ✅ Данные сохраняются навсегда
- ✅ Доступны из всех Worker instances
- ✅ Автоматическая репликация
- ✅ Бесплатно до 100,000 запросов/день
- ✅ Fallback на память, если KV не настроен

## Ограничения KV:

- ⚠️ Eventual consistency (данные могут быть не сразу доступны, обычно < 1 сек)
- ⚠️ Максимум 25MB на ключ
- ⚠️ Асинхронные операции (небольшая задержка)

