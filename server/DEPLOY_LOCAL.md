# Развертывание сервера на своем ПК для других пользователей

## Возможно ли это?

**Да, возможно!** Но есть несколько важных моментов:

## Что нужно для работы

### 1. Статический IP адрес или Dynamic DNS

**Проблема:** У большинства домашних интернет-провайдеров динамический IP, который меняется.

**Решения:**
- **Попросить у провайдера статический IP** (обычно платно, ~100-500₽/месяц)
- **Использовать Dynamic DNS** (бесплатно):
  - No-IP (https://www.noip.com/)
  - DuckDNS (https://www.duckdns.org/)
  - FreeDNS (https://freedns.afraid.org/)

### 2. Проброс портов (Port Forwarding)

**Что делать:**
1. Зайдите в настройки роутера (обычно 192.168.1.1 или 192.168.0.1)
2. Найдите "Port Forwarding" или "Виртуальные серверы"
3. Настройте проброс порта 3000 на ваш ПК:
   - Внешний порт: 3000
   - Внутренний IP: IP вашего ПК (например, 192.168.1.100)
   - Внутренний порт: 3000
   - Протокол: TCP

### 3. Настройка файрвола Windows

```powershell
# Откройте PowerShell от имени администратора
New-NetFirewallRule -DisplayName "Quiz Solver API" -Direction Inbound -LocalPort 3000 -Protocol TCP -Action Allow
```

### 4. Узнать внешний IP адрес

Откройте в браузере: https://whatismyipaddress.com/

## Пошаговая инструкция

### Шаг 1: Настройка Dynamic DNS (если нет статического IP)

1. Зарегистрируйтесь на https://www.duckdns.org/
2. Создайте домен (например: `my-quiz-server.duckdns.org`)
3. Установите обновляющий клиент на ПК (или используйте скрипт)

### Шаг 2: Настройка роутера

1. Зайдите в админ-панель роутера
2. Найдите "Port Forwarding"
3. Добавьте правило:
   ```
   Имя: Quiz Solver API
   Внешний порт: 3000
   Внутренний IP: [IP вашего ПК]
   Внутренний порт: 3000
   Протокол: TCP
   ```

### Шаг 3: Запуск сервера

```bash
cd f:\lms\server
npm install
npm start
```

### Шаг 4: Проверка доступности

Откройте в браузере на другом устройстве (не в вашей сети):
```
http://ваш-ip-адрес:3000/api/health
```

Или если используете Dynamic DNS:
```
http://my-quiz-server.duckdns.org:3000/api/health
```

### Шаг 5: Настройка расширения

В расширении укажите:
- URL: `http://ваш-ip-адрес:3000` или `http://my-quiz-server.duckdns.org:3000`

## ⚠️ ВАЖНО: Безопасность

### Проблемы домашнего сервера:

1. **Нет HTTPS** - данные передаются открытым текстом
2. **Нет защиты от DDoS** - могут перегрузить ваш интернет
3. **Доступ к вашей сети** - потенциальная уязвимость
4. **Нестабильность** - если ПК выключен, сервер недоступен

### Рекомендации:

1. **Используйте только для тестирования** или небольшой группы друзей
2. **Не используйте для продакшена** с большим количеством пользователей
3. **Настройте базовую защиту:**
   ```javascript
   // В server.js добавьте rate limiting
   const rateLimit = require('express-rate-limit');
   
   const limiter = rateLimit({
     windowMs: 15 * 60 * 1000, // 15 минут
     max: 100 // максимум 100 запросов
   });
   
   app.use('/api/', limiter);
   ```

## Альтернативы (рекомендуется)

### 1. Бесплатные хостинги (лучше для продакшена):

- **Railway** - бесплатный тариф, автоматическое развертывание
- **Render** - бесплатный тариф, простой интерфейс
- **Fly.io** - бесплатный тариф, хорошая производительность

### 2. VPS (если нужен контроль):

- **DigitalOcean** - от $5/месяц
- **Vultr** - от $2.50/месяц
- **Hetzner** - от €4/месяц

## Пример настройки для небольшой группы

Если вы хотите использовать только для друзей/одногруппников:

1. **Настройте Dynamic DNS** (DuckDNS - бесплатно)
2. **Откройте порт** в роутере
3. **Запустите сервер** на ПК
4. **Делитесь URL** только с доверенными людьми
5. **Используйте базовую авторизацию** через API ключ

## Проверка работы

После настройки проверьте:

1. **Локально:** `http://localhost:3000/api/health`
2. **Из интернета:** `http://ваш-ip:3000/api/health`
3. **Из расширения:** Включите синхронизацию и проверьте статус

## Итог

✅ **Можно** развернуть на своем ПК  
⚠️ **Но лучше** использовать облачный хостинг для надежности  
🔒 **Обязательно** настройте базовую защиту

