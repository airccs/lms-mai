# Команды для диагностики на сервере

## ✅ Проверено:
- UFW: неактивен (не блокирует)

## 🔍 Выполните следующие команды:

### 1. Проверка iptables

```bash
sudo iptables -L -n -v | head -30
```

Если есть правила, блокирующие порт 3000, временно отключите:
```bash
sudo iptables -F
sudo iptables -X
sudo iptables -t nat -F
sudo iptables -t nat -X
sudo iptables -t mangle -F
sudo iptables -t mangle -X
sudo iptables -P INPUT ACCEPT
sudo iptables -P FORWARD ACCEPT
sudo iptables -P OUTPUT ACCEPT
```

### 2. Проверка статуса сервиса

```bash
sudo systemctl status lms-api
```

### 3. Проверка, слушает ли сервер

```bash
sudo ss -tlnp | grep 3000
```

Должно быть: `0.0.0.0:3000` или `*:3000`

### 4. Тест локального подключения

```bash
curl -v http://localhost:3000/api/health
```

### 5. Тест через порт 8080

Остановите сервис и запустите на порту 8080:

```bash
# Остановите сервис
sudo systemctl stop lms-api

# Запустите на порту 8080
cd ~/lms-server/server
PORT=8080 node server.js
```

Затем с вашего компьютера попробуйте: `http://130.61.200.70:8080/api/health`

**Если порт 8080 работает** - проблема может быть в Oracle Cloud блокировке порта 3000.

### 6. Мониторинг трафика (tcpdump)

В отдельном окне SSH:

```bash
# Установите tcpdump (если нет)
sudo apt-get update
sudo apt-get install -y tcpdump

# Запустите мониторинг
sudo tcpdump -i any -n port 3000
```

Затем с вашего компьютера попробуйте подключиться. Если пакеты не появляются - они блокируются до сервера.

### 7. Проверка вкладки Security в VNIC

В Oracle Cloud Console:

1. На странице VNIC перейдите на вкладку **Security**
2. Проверьте раздел **Security Lists**
3. Убедитесь, что привязан правильный Security List (тот, где есть правило для порта 3000)
4. Если есть несколько Security Lists - проверьте все

## 🎯 Следующий шаг:

Выполните команды 1-4 и пришлите результаты. Особенно важно проверить iptables и попробовать порт 8080.

