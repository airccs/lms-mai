# Исправление настройки проброса портов

## Проблема в текущей настройке:

В поле **"Вход"** указан локальный IP: `192.168.1.63/32`

Это **неправильно**! Поле "Вход" должно указывать на источник входящего трафика из интернета, а не на локальный IP.

## Как исправить:

### Вариант 1: Удалить и создать заново

1. Нажмите на правило (или кнопку редактирования)
2. Удалите текущее правило
3. Создайте новое правило:

**Правильные настройки:**
- **Вход:** Оставьте пустым ИЛИ выберите "WAN" / "Интернет" / "Другой адрес" с IP `0.0.0.0`
- **Выход:** `DESKTOP-NCEQ95M` (192.168.1.63) - это правильно!
- **Порты:** `TCP/3000` - правильно!
- **Описание:** `Quiz Solver API` - правильно!

### Вариант 2: Отредактировать существующее

1. Нажмите на правило для редактирования
2. В поле **"Вход"**:
   - Выберите "Другой адрес"
   - В поле IP-адрес: оставьте пустым ИЛИ введите `0.0.0.0`
   - Маска подсети: `255.255.255.255 (/32)`
3. Сохраните изменения

## Правильная конфигурация:

```
✅ Включить правило: [ВКЛЮЧЕНО]
📥 Вход: [Пусто] или [0.0.0.0] или [WAN/Интернет]
📤 Выход: DESKTOP-NCEQ95M (192.168.1.63) ✅
🔌 Порты: TCP/3000 ✅
📝 Описание: Quiz Solver API ✅
```

## После исправления:

1. Сохраните изменения
2. Подождите 1-2 минуты
3. Проверьте доступность:
   ```
   http://95.24.148.25:3000/api/health
   ```
4. Если работает - проверьте через доменное имя:
   ```
   http://lms-solver.ddns.net:3000/api/health
   ```

## Объяснение:

- **Вход** = откуда приходит трафик (из интернета = пусто/0.0.0.0)
- **Выход** = куда перенаправлять (ваш ПК = 192.168.1.63) ✅

Текущая настройка пытается принимать трафик только с локального IP, что не имеет смысла для доступа из интернета.

