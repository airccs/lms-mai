# Полная диагностика проблемы подключения

## Проблема: Запросы не доходят до сервера (нет записей в логах)

Если при попытке подключения извне в логах не появляются записи, запросы блокируются на уровне сети.

## ✅ Что уже проверено:

1. ✅ Сервер работает локально (`curl http://localhost:3000/api/health`)
2. ✅ Сервер слушает на `0.0.0.0:3000` (проверено через `ss -tlnp`)
3. ✅ Security List имеет правило для порта 3000
4. ✅ Route Table настроен правильно
5. ✅ Internet Gateway прикреплен
6. ✅ Subnet - Public Subnet
7. ✅ Instance имеет Public IP

## 🔍 Что нужно проверить:

### 1. Network Security Groups (NSG) - ПРИОРИТЕТ #1

**Это самая частая причина!** NSG работает поверх Security List и может блокировать весь трафик.

#### Проверка:

```bash
# На сервере проверьте, какие NSG привязаны к VNIC
# Это нужно сделать через Oracle Cloud Console:
```

1. **Compute** → **Instances** → `lms-api-server`
2. **Attached VNICs** → нажмите на VNIC
3. Проверьте раздел **Network Security Groups**
4. Если есть NSG - перейдите в **Networking** → **Network Security Groups**
5. Проверьте **Ingress Rules** - должно быть правило для порта 3000

**Если правила нет - добавьте:**
- Source: `0.0.0.0/0`
- Protocol: TCP
- Port: 3000

**Или отключите NSG**, если он не нужен (уберите из VNIC).

### 2. Firewall на уровне ОС (UFW)

```bash
# Проверьте статус UFW
sudo ufw status

# Если UFW активен, откройте порт
sudo ufw allow 3000/tcp
sudo ufw reload

# Проверьте, что правило добавлено
sudo ufw status numbered
```

### 3. iptables (если используется)

```bash
# Проверьте правила iptables
sudo iptables -L -n -v | grep 3000

# Если порт заблокирован, откройте его
sudo iptables -A INPUT -p tcp --dport 3000 -j ACCEPT
sudo iptables-save
```

### 4. Проверка с другого порта

Попробуйте временно изменить порт на 8080 (уже есть правило в Security List):

```bash
# Остановите сервис
sudo systemctl stop lms-api

# Запустите на порту 8080
cd ~/lms-server/server
PORT=8080 node server.js
```

Затем попробуйте: `http://130.61.200.70:8080/api/health`

Если работает - проблема специфична для порта 3000.

### 5. Проверка через telnet

С вашего компьютера попробуйте:

```bash
# Windows PowerShell
Test-NetConnection -ComputerName 130.61.200.70 -Port 3000

# Linux/Mac
telnet 130.61.200.70 3000
# или
nc -zv 130.61.200.70 3000
```

Если соединение не устанавливается - порт заблокирован на уровне сети.

### 6. Проверка через tcpdump (на сервере)

```bash
# Установите tcpdump (если нет)
sudo apt-get update
sudo apt-get install -y tcpdump

# Запустите мониторинг трафика на порту 3000
sudo tcpdump -i any -n port 3000

# В другом окне попробуйте подключиться извне
# Если пакеты не появляются - они блокируются до сервера
```

### 7. Проверка через Oracle Cloud VCN Flow Logs

1. **Networking** → **Virtual Cloud Networks** → ваша VCN
2. **Flow Logs** → включите логирование (если еще не включено)
3. Попробуйте подключиться извне
4. Проверьте логи - видны ли запросы

### 8. Проверка через другой протокол

Попробуйте временно открыть порт 80 (HTTP):

```bash
# Добавьте правило в Security List для порта 80
# Измените PORT в server.js на 80
# Перезапустите сервис
```

Если порт 80 работает, а 3000 нет - проблема может быть в Oracle Cloud блокировке нестандартных портов.

## 🎯 Пошаговый план действий:

1. **Сначала проверьте NSG** (самая частая причина)
2. **Проверьте UFW** на сервере
3. **Попробуйте порт 8080** (уже есть правило)
4. **Проверьте через telnet** с вашего компьютера
5. **Запустите tcpdump** на сервере для диагностики

## 📝 Команды для быстрой проверки на сервере:

```bash
# 1. Проверка UFW
sudo ufw status

# 2. Проверка iptables
sudo iptables -L -n -v | grep 3000

# 3. Проверка, слушает ли сервер
sudo ss -tlnp | grep 3000

# 4. Проверка через tcpdump (в отдельном окне)
sudo tcpdump -i any -n port 3000

# 5. Проверка логов сервиса
sudo journalctl -u lms-api -f
```

## 🔧 Быстрое решение (если NSG проблема):

1. **Compute** → **Instances** → `lms-api-server`
2. **Attached VNICs** → VNIC → **Edit**
3. Уберите все NSG из списка
4. Сохраните
5. Подождите 2-3 минуты
6. Попробуйте подключиться снова

