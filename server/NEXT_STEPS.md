# Следующие шаги диагностики

## ✅ Что уже проверено:

1. ✅ NSG не используется (прочерк в Network Security Groups)
2. ✅ Security List имеет правило для порта 3000
3. ✅ Route Table настроен правильно
4. ✅ Public IP назначен: `130.61.200.70`
5. ✅ Subnet: Public Subnet
6. ✅ Сервер слушает на `0.0.0.0:3000`

## 🔍 Что проверить дальше:

### 1. Проверка Security List в VNIC

В Oracle Cloud Console:

1. На странице VNIC перейдите на вкладку **Security**
2. Проверьте, какой **Security List** привязан к VNIC
3. Убедитесь, что это тот же Security List, где есть правило для порта 3000

### 2. Проверка firewall на сервере (UFW)

Выполните на сервере:

```bash
# Проверьте статус UFW
sudo ufw status

# Если UFW активен, откройте порт 3000
sudo ufw allow 3000/tcp
sudo ufw reload

# Проверьте правила
sudo ufw status numbered
```

### 3. Проверка iptables

```bash
# Проверьте правила iptables
sudo iptables -L -n -v | grep 3000

# Если порт заблокирован, откройте его
sudo iptables -A INPUT -p tcp --dport 3000 -j ACCEPT
sudo iptables-save

# Или временно отключите iptables для теста
sudo iptables -F
```

### 4. Тест через порт 8080

В Security List уже есть правило для порта 8080. Попробуйте:

```bash
# Остановите сервис
sudo systemctl stop lms-api

# Запустите на порту 8080 вручную
cd ~/lms-server/server
PORT=8080 node server.js
```

Затем попробуйте: `http://130.61.200.70:8080/api/health`

**Если порт 8080 работает, а 3000 нет** - проблема может быть в Oracle Cloud блокировке порта 3000.

### 5. Проверка через tcpdump (на сервере)

```bash
# Установите tcpdump (если нет)
sudo apt-get update
sudo apt-get install -y tcpdump

# Запустите мониторинг трафика на порту 3000
sudo tcpdump -i any -n port 3000
```

В другом окне попробуйте подключиться извне. Если пакеты не появляются - они блокируются до сервера.

### 6. Проверка через telnet (с вашего компьютера)

**Windows PowerShell:**
```powershell
Test-NetConnection -ComputerName 130.61.200.70 -Port 3000
```

**Linux/Mac:**
```bash
telnet 130.61.200.70 3000
# или
nc -zv 130.61.200.70 3000
```

Если соединение не устанавливается - порт заблокирован на уровне сети.

### 7. Проверка вкладки Security в VNIC

В Oracle Cloud Console:

1. На странице VNIC перейдите на вкладку **Security**
2. Проверьте раздел **Security Lists**
3. Убедитесь, что привязан правильный Security List (тот, где есть правило для порта 3000)

## 🎯 Рекомендуемый порядок действий:

1. **Проверьте вкладку Security в VNIC** - убедитесь, что правильный Security List привязан
2. **Проверьте UFW на сервере** - выполните команды выше
3. **Попробуйте порт 8080** - если работает, значит проблема специфична для порта 3000
4. **Запустите tcpdump** - чтобы увидеть, доходят ли пакеты до сервера

## 📝 Быстрая проверка на сервере:

Выполните эти команды на сервере и пришлите результаты:

```bash
# 1. Проверка UFW
sudo ufw status

# 2. Проверка iptables
sudo iptables -L -n -v | head -20

# 3. Проверка, слушает ли сервер
sudo ss -tlnp | grep 3000

# 4. Проверка статуса сервиса
sudo systemctl status lms-api
```

