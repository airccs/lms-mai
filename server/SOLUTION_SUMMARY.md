# Решение проблемы ERR_CONNECTION_TIMED_OUT

## ✅ Проблема решена!

Сервер теперь доступен извне по адресу: `http://130.61.200.70:3000`

## 🔍 Причина проблемы:

**iptables блокировал трафик на порт 3000**. В цепочке INPUT было правило `REJECT`, которое блокировало весь трафик, не соответствующий предыдущим правилам. Порт 3000 не был разрешен, поэтому все запросы блокировались.

## 🛠️ Решение:

Добавлено правило в iptables для разрешения трафика на порт 3000:

```bash
sudo iptables -I INPUT 5 -p tcp --dport 3000 -j ACCEPT
```

## 💾 Сохранение правила:

Чтобы правило не пропало после перезагрузки:

```bash
# Установите iptables-persistent
sudo apt-get update
sudo apt-get install -y iptables-persistent

# Сохраните правила
sudo netfilter-persistent save
```

## ✅ Что было проверено:

1. ✅ Security List - правило для порта 3000 есть
2. ✅ Route Table - настроен правильно
3. ✅ Internet Gateway - прикреплен
4. ✅ Subnet - Public Subnet
5. ✅ Public IP - назначен
6. ✅ NSG - не используется
7. ✅ UFW - неактивен
8. ✅ **iptables - было правило REJECT, блокирующее порт 3000** ← ПРОБЛЕМА
9. ✅ Сервер слушает на `0.0.0.0:3000`

## 📝 Важные замечания:

- **iptables** в Oracle Cloud Ubuntu образе по умолчанию имеет правило `REJECT`, которое блокирует весь трафик, кроме явно разрешенного
- По умолчанию разрешены только: SSH (22), ICMP, loopback, и существующие соединения
- Для любого нового порта нужно явно добавлять правило в iptables

## 🔄 Если нужно добавить другие порты:

```bash
# Добавьте правило для нового порта
sudo iptables -I INPUT 5 -p tcp --dport <PORT> -j ACCEPT

# Сохраните
sudo netfilter-persistent save
```

## 🌐 Текущий статус:

- **API URL**: `http://130.61.200.70:3000`
- **Health Check**: `http://130.61.200.70:3000/api/health`
- **Статус**: ✅ Работает

## 📚 Связанные документы:

- `FIX_IPTABLES.md` - подробная инструкция по исправлению iptables
- `ORACLE_DEPLOY.md` - полная инструкция по развертыванию на Oracle Free Tier
- `TROUBLESHOOTING.md` - общее руководство по устранению проблем

