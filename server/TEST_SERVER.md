# Проверка работы сервера

## Ваш сервер доступен по адресу:
```
http://lms-solver.ddns.net:3000
```

## Шаги для проверки:

### 1. Проверьте локально (на вашем ПК)
Откройте в браузере:
```
http://localhost:3000/api/health
```

Должен вернуться ответ:
```json
{
  "status": "ok",
  "message": "Quiz Solver API is running"
}
```

### 2. Проверьте через доменное имя (на вашем ПК)
Откройте в браузере:
```
http://lms-solver.ddns.net:3000/api/health
```

### 3. Проверьте из интернета
- С телефона (через мобильный интернет, НЕ Wi-Fi)
- Или попросите друга открыть:
```
http://lms-solver.ddns.net:3000/api/health
```

### 4. Проверьте все endpoints

#### Health Check:
```
http://lms-solver.ddns.net:3000/api/health
```

#### Статистика сервера:
```
http://lms-solver.ddns.net:3000/api/server/stats
```

#### Вся статистика:
```
http://lms-solver.ddns.net:3000/api/stats
```

## Настройка расширения:

1. Откройте popup расширения (кликните на иконку)
2. Найдите раздел "🌐 Синхронизация между пользователями"
3. Включите "Синхронизацию с сервером"
4. В поле "URL API сервера" введите:
   ```
   http://lms-solver.ddns.net:3000
   ```
5. Нажмите "Сохранить настройки"
6. Должен появиться статус: "✅ Соединение установлено"

## Если не работает:

### Проблема: "Не удалось подключиться"
- Проверьте, что сервер запущен (`npm start`)
- Проверьте, что порт 3000 открыт в брандмауэре
- Проверьте, что проброс портов настроен в роутере

### Проблема: "Сервер недоступен"
- Убедитесь, что ПК включен
- Проверьте, что роутер включен
- Проверьте, что Dynamic DNS обновил IP адрес

### Проблема: "Таймаут"
- Проверьте, что внешний IP в Dynamic DNS совпадает с текущим
- Попробуйте обновить IP в настройках Dynamic DNS

## Проверка через curl (если установлен):

```bash
curl http://lms-solver.ddns.net:3000/api/health
```

Должен вернуться:
```json
{"status":"ok","message":"Quiz Solver API is running"}
```

