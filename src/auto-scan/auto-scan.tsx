import React from 'react';
import ReactDOM from 'react-dom/client';
import AutoScanApp from './AutoScanApp';
import '../saved-data/saved-data.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AutoScanApp />
  </React.StrictMode>
);

